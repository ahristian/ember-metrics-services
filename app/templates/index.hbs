{{#each this.model.readings.data as |meter|}}
  <div class="mt-2 text-md">
    Type Value: {{meter.type}}
  </div>
  <div class="mt-2 text-md">
    Id: {{meter.id}}
  </div>
  <div class="mt-2 text-md">
    KW Values
  </div>
  <BarPlot @metrics={{meter.attributes.readings.kw}} />
{{/each}}

<Table @services={{this.model.services.data}} />

{{outlet}}